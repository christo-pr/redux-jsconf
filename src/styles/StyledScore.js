import styled from "styled-components"

export const StyledScore = styled.div`
  display: flex;
  justify-content: flex-end;
  align-items: center;
  margin-top: 1rem;

  p {
    margin-right: 1rem;
    font-size: 30px;
  }
`
